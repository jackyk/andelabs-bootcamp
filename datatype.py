
def data_type(structure):

    if a is None:
        return "no value"

    elif type(structure) == str :
        return len(structure)

    elif type(structure) == bool:
        return structure

    ele type(structure) == int:
        if structure <= 100:

        return "Less than 100"
    else:
        return "More than 100"

    if type(structure) == list:
        if len(structure) < 3
            return None
    else:
            return a[2]


print data_type("Keem")
print data_type(None)
print data_type(True)
print data_type(145)


'''def data_type(a):

    if a is  None:
      return "no value"

    elif type(a) == str:
      return len(a)

    elif type(a) == bool:
      return a

    elif type(a) == int:

        if a >= 100:
            return "more than 100"
        else:
            return "less than 100"

    elif type(a) == list:
        if len(a) < 3:
            return None
        else:
            return a[2]'''
